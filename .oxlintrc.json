{
  "$schema": "https://raw.githubusercontent.com/oxc-project/oxc/main/npm/oxlint/configuration_schema.json",
  "categories": {
    "correctness": "error"
  },
  "ignorePatterns": [
    "**/dist/**",
    // Example React snippets are ?raw-loaded display sources for the docs
    // code panel, not modules - bare adjacent JSX is the point.
    "apps/site/src/examples/**/*.tsx"
  ]
}
