dist
pnpm-lock.yaml
node_modules
docs/summaries
docs/plans
# Agent worktrees are separate checkouts; never format them from here.
.claude/worktrees
# Example React snippets are ?raw-loaded display sources for the docs code
# panel, not modules - bare adjacent JSX does not parse as TSX.
apps/site/src/examples/**/*.tsx

