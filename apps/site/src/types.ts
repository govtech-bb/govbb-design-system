// Top-level site sections. Single source of truth for the nav union — used by
// the header (active state) and the layouts.
export type Section =
  'components' | 'documentation' | 'ai-skills' | 'changelog';
