commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}

pre-commit:
  commands:
    lint:
      run: pnpm lint
    format:
      run: pnpm format:check

pre-push:
  commands:
    typecheck:
      run: pnpm -r typecheck
    test:
      run: pnpm -r test
